import { ElTypeFilterPipe } from './el-type-filter.pipe';

describe('ElTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ElTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
